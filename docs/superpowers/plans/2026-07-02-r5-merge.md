# R5 — Merge Engine (M4) Implementation Plan

**Goal:** Manual + random merge of `in_pool` videos → ffmpeg output in `data/finals/`.

**Stories:** US-041

## Tasks

1. Story US-041 + harness
2. Alembic 0006: `merge_jobs`, `final_videos`, extend `review_status` with `merged`
3. `services/merge.py` — ffmpeg concat, mock-friendly
4. `workers/merge_job.py` — orchestrate merge, persist finals
5. API: pool, manual/random merge, job status, list finals
6. Web `/merge` UI + nav
7. Tests mocked ffmpeg
8. Docs + progress report
