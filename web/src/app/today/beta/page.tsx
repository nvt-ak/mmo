import { InboxPage } from "@/components/inbox/inbox-page";

export default function BetaInboxPage() {
  return (
    <InboxPage
      keywordType="beta"
      title="Beta inbox"
      description="Long-tail Creator Rewards keywords — full agent + TikTok gate."
      discoveryFilter="beta"
    />
  );
}
