import { InboxPage } from "@/components/inbox/inbox-page";

export default function NurtureInboxPage() {
  return (
    <InboxPage
      keywordType="nurture"
      title="Nurture inbox"
      description="Broad trend keywords — light TikTok gate, quick approve/reject."
      discoveryFilter="nurture"
    />
  );
}
